import TrackOrderClient from "@/components/TrackOrderClient";

export const metadata = {
  title: "Track Order",
  description: "Track the status of your Culinary Hub order.",
};

export default function TrackOrderPage() {
  return <TrackOrderClient />;
}